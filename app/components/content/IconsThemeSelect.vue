<script setup lang="ts">
const { icon, icons } = useTheme()
</script>

<template>
  <UFormField :label="$t('theme.preset')">
    <USelect
      v-model="icon"
      color="neutral"
      :icon="icons.find(i => i.value === icon)?.icon"
      :items="icons"
      class="min-w-50"
    />
  </UFormField>
</template>
